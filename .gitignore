# Credentials — also catches editor backups like .env~ and .env.local
.env*
!.env.example
*.pyc
__pycache__/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Packaging artifacts (pip install -e . / python -m build)
build/
dist/
*.egg-info/
*.csv
*.json
